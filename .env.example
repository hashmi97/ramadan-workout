# Optional: enable passgate to protect the app with a password
# Set to "true" or "1" to require a password before accessing the app
VITE_PASSGATE_ENABLED=false
# Required when passgate is enabled
VITE_PASSGATE_PASSWORD=your_password
